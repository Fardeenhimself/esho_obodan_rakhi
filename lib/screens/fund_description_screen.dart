import 'package:flutter/material.dart';

class FundDescriptionScreen extends StatelessWidget {
  const FundDescriptionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('এসো অবদান রাখি')),
      body: Center(child: Text('FUND DETAILS')),
    );
  }
}
